# APT Package Manager

Update package list

```bash
sudo apt update
```

Upgrade packages

```bash
sudo apt upgrade
```

Install package

```bash
sudo apt install nginx
```

Remove package

```bash
sudo apt remove nginx
```